# Server
PORT=8787
HOST=0.0.0.0

# DashScope realtime ASR. If omitted, the server uses mock transcript mode.
DASHSCOPE_API_KEY=

# OpenAI-compatible chat completions endpoint. If omitted, recovery uses a local fallback.
LLM_API_KEY=
LLM_BASE_URL=https://api.openai.com/v1
LLM_MODEL=gpt-4o-mini
